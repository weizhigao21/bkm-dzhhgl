# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.7] - 2026-08-02

### 新增
- 训练师信息新增「最近兑换」展示：显示最近一次兑换的时间与兑换码，兑换成功后自动记录并持久化（保留最近 50 条）
- 到期时间显示优化：时间戳格式化为日期并附带剩余天数

### 变更
- 到期时间颜色提醒：剩余 ≥ 7 天绿色、< 7 天橙色、已过期红色
- 训练师信息移除「刷新时间」行

### 修复
- 套餐名称不显示：API 返回中套餐名位于 `plan.name`，修正提取逻辑
- 训练师列表剩余流量显示精度错误：59.99 GB 被 `:.1f` 四舍五入显示为 60.0 GB，改为两位小数

## [1.6]

### 新增
- 邮件通知：SMTP 定时检查流量，低于阈值自动发邮件提醒
- 每日报告：每天定时发送所有账号流量汇总邮件
- 开机自启动：通过 Windows 注册表实现
- 设置弹窗：邮箱通知 / 开机自启统一配置

### 变更
- 授权码加密存储（机器绑定）

## [1.5]

### 新增
- 全部兑换：一键给所有训练师并行兑换（6 线程），结果汇总弹窗展示
- 兑换码历史下拉框，自动记录使用过的兑换码
- `user_list_item.py` 训练师列表项组件

### 变更
- 密码加密存储（机器绑定）
- 全局刷新改为并行
- Token 重试冷却防抖
- 线程安全锁保护共享状态
- 兑换流程抽取复用

## [1.4]

### 变更
- 取消 Token 输入框，改用登录代替
- 代码拆分至 `src/` 目录

### 修复
- 缓存数据不显示、切换用户不刷新等问题

## [1.3]

### 新增
- 账号密码持久化存储
- Token 过期自动登录
- 用户点击防抖、请求冲突处理

## [1.2]

### 新增
- 全局刷新、刷新时间显示

### 变更
- 缓存破坏问题处理

## [1.1]

### 新增
- 右键删除训练师
- 窗口屏幕居中

## [1.0]

### 新增
- 初始版本：多账号登录 / 切换 / 查询 / 兑换码
